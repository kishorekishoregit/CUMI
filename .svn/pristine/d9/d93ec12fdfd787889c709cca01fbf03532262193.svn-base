using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using JKWOODSENTITY;
using System.Transactions;
using System.Data.SqlClient;
using System.Data;
using System.Web.Script.Serialization;

namespace JKWOODSDAC
{
    public partial class WMSDAL
    {
        public ResponseCustomerMaster FetchCustomerMasterPageLoadDAL()
        {
            ResponseCustomerMaster response = new ResponseCustomerMaster();
            response.ErrorContainer = new List<ErrorItem>();
            try
            {
                using (TransactionScope scope = new TransactionScope())
                {
                    using (SqlConnection con = new SqlConnection(connectionstring))
                    {
                        SqlCommand cmd = new SqlCommand("Masters.CustomerMaster_Fetch", con);
                        cmd.CommandType = CommandType.StoredProcedure;
                        con.Open();
                        SqlDataAdapter oda = new SqlDataAdapter(cmd);
                        DataSet ds = new DataSet();
                        oda.Fill(ds);
                        if (ds != null)
                        {

                            response.JS_RecordStatus = ds.Tables[0];
                            response.JS_Customerdetails = ds.Tables[1];
                            response.result = true;
                        }
                        scope.Complete();
                    }
                }


            }
            catch (Exception ex)
            {
                string responsetime = DateTime.Now.ToString("yyyy MM dd hh:mm:ss.fff tt");
                string responselog = createlog("FetchCustomerMasterPageLoadDAL: " + "Method Name FetchCustomerMasterPageLoadDAL" + " -- " + ex.StackTrace + "-- " + ex.Source + " -- " + ex.Message);
                response.ErrorContainer.Add(new ErrorItem { DataItem = responselog, ErrorNo = "LWMS00000" });
                response.result = false;
            }


            return response;
        }
        public ResponseCustomerMaster InsertCustomerMasterDAL(RequestCustomerMaster request)
        {
            ResponseCustomerMaster response = new ResponseCustomerMaster();
            response.ErrorContainer = new List<ErrorItem>();
            try
            {
                using (TransactionScope scope = new TransactionScope())
                {
                    using (SqlConnection con = new SqlConnection(connectionstring))
                    {
                        SqlCommand cmd = new SqlCommand("Masters.CustomerMaster_Insert", con);
                        cmd.CommandType = CommandType.StoredProcedure;
                        cmd.Parameters.Add(new SqlParameter("@CUSTOMERCODE", request.requestCustomerMaster.CUSTOMERCODE.Trim()));
                        cmd.Parameters.Add(new SqlParameter("@CUSTOMERNAME", request.requestCustomerMaster.CUSTOMERNAME.Trim()));
                        cmd.Parameters.Add(new SqlParameter("@CUSTOMERADDRESS", request.requestCustomerMaster.CUSTOMERADDRESS.Trim()));
                        cmd.Parameters.Add(new SqlParameter("@CUSTOMERSTATUS", request.requestCustomerMaster.CUSTOMERSTATUS.Trim()));
                        cmd.Parameters.Add(new SqlParameter("@USERCODE", request.requestCustomerMaster.USERCODE.Trim()));
                        con.Open();
                        SqlDataAdapter oda = new SqlDataAdapter(cmd);
                        DataSet ds = new DataSet();
                        oda.Fill(ds);
                        if (ds != null)
                        {
                            if (ds.Tables[0].Rows[0][0].ToString() == "SUCCESS")
                                response.result = true;
                            else
                                response.result = false;

                            response.JS_Customerdetails = ds.Tables[1];
                            response.ErrorContainer.Add(new ErrorItem { ErrorNo = ds.Tables[0].Rows[0][1].ToString(), DataItem = request.requestCustomerMaster.CUSTOMERCODE });
                        }
                    }
                    scope.Complete();
                }
            }
            catch (Exception ex)
            {

                string responsetime = DateTime.Now.ToString("yyyy MM dd hh:mm:ss.fff tt");
                string responselog = createlog("InsertCustomerMasterDAL: " + "Method Name InsertCustomerMasterDAL" + " -- " + ex.StackTrace + "-- " + ex.Source + " -- " + ex.Message);
                response.ErrorContainer.Add(new ErrorItem { DataItem = responselog, ErrorNo = "LWMS00000" });
                response.result = false;

            }



            return response;

        }
        public ResponseCustomerMaster EditCustomerMasterbyDAL(RequestCustomerMaster request)
        {
            ResponseCustomerMaster response = new ResponseCustomerMaster();
            response.ErrorContainer = new List<ErrorItem>();
            try
            {
                using (TransactionScope scope = new TransactionScope())
                {
                    using (SqlConnection con = new SqlConnection(connectionstring))
                    {
                        SqlCommand cmd = new SqlCommand("Masters.CustomerMaster_EditCustomerDetails", con);
                        cmd.CommandType = CommandType.StoredProcedure;
                        cmd.Parameters.Add(new SqlParameter("@ID", request.requestCustomerMaster.HDCUS.Trim()));
                        con.Open();
                        SqlDataAdapter oda = new SqlDataAdapter(cmd);
                        DataSet ds = new DataSet();
                        oda.Fill(ds);
                        if (ds != null)
                        {
                            response.JS_Customerdetails = ds.Tables[0];
                            response.result = true;
                        }

                    }
                    scope.Complete();
                }
            }
            catch (Exception ex)
            {

                string responsetime = DateTime.Now.ToString("yyyy MM dd hh:mm:ss.fff tt");
                string responselog = createlog("EditCustomerMasterbyDAL: " + "Method Name EditCustomerMasterbyDAL" + " -- " + ex.StackTrace + "-- " + ex.Source + " -- " + ex.Message);
                response.ErrorContainer.Add(new ErrorItem { DataItem = responselog, ErrorNo = "LWMS00000" });
                response.result = false;

            }


            return response;

        }
        public ResponseCustomerMaster UpdateCustomerMasterDAL(RequestCustomerMaster request)
        {
            ResponseCustomerMaster response = new ResponseCustomerMaster();
            response.ErrorContainer = new List<ErrorItem>();
            try
            {
                using (TransactionScope scope = new TransactionScope())
                {
                    using (SqlConnection con = new SqlConnection(connectionstring))
                    {
                        SqlCommand cmd = new SqlCommand("Masters.CustomerMaster_Update", con);
                        cmd.CommandType = CommandType.StoredProcedure;
                        cmd.Parameters.Add(new SqlParameter("@ID", request.requestCustomerMaster.HDCUS.Trim()));
                        cmd.Parameters.Add(new SqlParameter("@CUSTOMERCODE", request.requestCustomerMaster.CUSTOMERCODE.Trim()));
                        cmd.Parameters.Add(new SqlParameter("@CUSTOMERNAME", request.requestCustomerMaster.CUSTOMERNAME.Trim()));
                        cmd.Parameters.Add(new SqlParameter("@CUSTOMERADDRESS", request.requestCustomerMaster.CUSTOMERADDRESS.Trim()));
                        cmd.Parameters.Add(new SqlParameter("@CUSTOMERSTATUS", request.requestCustomerMaster.CUSTOMERSTATUS.Trim()));
                        cmd.Parameters.Add(new SqlParameter("@USERCODE", request.requestCustomerMaster.USERCODE.Trim()));
                        con.Open();
                        SqlDataAdapter oda = new SqlDataAdapter(cmd);
                        DataSet ds = new DataSet();
                        oda.Fill(ds);
                        if (ds != null)
                        {
                            if (ds.Tables[0].Rows[0][0].ToString() == "SUCCESS")
                                response.result = true;
                            else
                                response.result = false;

                            response.JS_Customerdetails = ds.Tables[1];
                            response.ErrorContainer.Add(new ErrorItem { ErrorNo = ds.Tables[0].Rows[0][1].ToString(), DataItem = request.requestCustomerMaster.CUSTOMERCODE });
                        }
                    }
                    scope.Complete();
                }
            }
            catch (Exception ex)
            {

                string responsetime = DateTime.Now.ToString("yyyy MM dd hh:mm:ss.fff tt");
                string responselog = createlog("UpdateCustomerMasterDAL: " + "Method Name UpdateCustomerMasterDAL" + " -- " + ex.StackTrace + "-- " + ex.Source + " -- " + ex.Message);
                response.ErrorContainer.Add(new ErrorItem { DataItem = responselog, ErrorNo = "LWMS00000" });
                response.result = false;

            }



            return response;

        }
    }
}
